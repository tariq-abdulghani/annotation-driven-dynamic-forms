import { InputTemplateDirective } from './input-template.directive';

describe('InputTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
