import { ButtonTemplateDirective } from './button-template.directive';

describe('ButtonTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
