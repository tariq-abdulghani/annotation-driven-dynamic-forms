import { InputAnchorDirective } from './input-anchor.directive';

describe('InputAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
